import 'package:flutter/material.dart';
import 'package:slivers_test/post.dart';

class ListViewCustom extends StatelessWidget {
  final List<Post> posts;

  const ListViewCustom({
    super.key,
    required this.posts,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
